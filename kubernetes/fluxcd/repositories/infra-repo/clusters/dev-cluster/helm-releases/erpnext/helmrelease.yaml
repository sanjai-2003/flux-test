apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: erpnext
  namespace: erpnext
spec:
  releaseName: erpnext
  chart:
    spec:
      chart: erpnext
      version: 2.0.11
      sourceRef:
        kind: HelmRepository
        name: frappe
  valuesFrom:
    - kind: External
      name: https://raw.githubusercontent.com/VeryStrongFingers/erpnext-k3s/master/helm-charts/erpnext-values.yaml

