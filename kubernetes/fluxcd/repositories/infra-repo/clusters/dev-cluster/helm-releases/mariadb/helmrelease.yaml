apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mariadb
  namespace: mariadb
spec:
  releaseName: mariadb
  chart:
    spec:
      chart: mariadb
      version: 11.0.10
      sourceRef:
        kind: HelmRepository
        name: bitnami
  valuesFrom:
    - kind: External
      name: https://raw.githubusercontent.com/VeryStrongFingers/erpnext-k3s/master/helm-charts/maria-db-values.yaml

